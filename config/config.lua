Config = Config or {}

Config.CheckForUpdates = false

Config.HudMainColor = '191, 175, 131'       -- rgb(40, 170, 142)
Config.PlayerChangeableHudMainColor = true -- Whether the player can change the color of the HUD.

Config.DisplayServerLogo = false           -- Display the server logo in the top right corner of the screen. Change the logo from `build/svlogo.png`.
Config.LogoPosition = 'right'              -- The position of the server logo. *Can be `left` or `right`.

Config.ShowPlayerInfo = false               -- Whether to show the player info (job, cash, bank, etc).
